% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_prep.R
\name{prepare_multirole_model}
\alias{prepare_multirole_model}
\title{Prepare the multi-role workload allocation model}
\usage{
prepare_multirole_model(
  df_list,
  ta_protected_max = 1,
  gr_protected_max = 1,
  e_max = NULL,
  ta_min = NULL,
  ta_max = NULL,
  gr_min = NULL,
  gr_max = NULL,
  e_min = NULL,
  alpha_ta = 2,
  alpha_gr = NULL,
  beta_ta = 1,
  beta_gr = NULL,
  phi = 1,
  rho_ta = 10,
  rho_gr = NULL,
  C = 4,
  protected_year_ta = 1,
  protected_year_gr = 1
)
}
\arguments{
\item{df_list}{A model input list from \code{\link[=extract_multirole_info]{extract_multirole_info()}}.}

\item{ta_protected_max, gr_protected_max}{Non-negative soft upper limits on
current-semester TA or GR workload for the corresponding protected cohort.
A value may be \code{NULL} when the corresponding \verb{rho_*} term is disabled.}

\item{e_max}{Optional upper bound on per-individual E units.}

\item{ta_min, ta_max}{Optional lower and upper bounds on per-individual TA
units.}

\item{gr_min, gr_max}{Optional lower and upper bounds on per-individual GR
units.}

\item{e_min}{Optional lower bound on per-individual E units.}

\item{alpha_ta, alpha_gr}{Non-negative weights for annual TA and GR workload
spread.}

\item{beta_ta, beta_gr}{Non-negative weights for TA and GR preferences.}

\item{phi}{Non-negative weight for score-guided E allocation.}

\item{rho_ta, rho_gr}{Non-negative penalties for TA and GR protected-cohort
slack.}

\item{C}{Semester workload capacity per individual. Annual total workload is
fixed at \code{2 * C}.}

\item{protected_year_ta, protected_year_gr}{Whole numbers from 1 to 4
identifying the TA- and GR-protected cohorts.}
}
\value{
An \code{ompr} model.
}
\description{
Builds a mixed-integer model for assigning TA, GR, and lighter E duties
while balancing role-specific workload, preferences, and cohort protection.
}
\details{
Any objective weight set to \code{NULL} or zero is disabled. Disabled preference
and E terms are omitted from the objective. Disabling a spread term also
omits its two spread variables and fairness constraints. Disabling a
protection penalty omits that role's slack variables and soft-limit
constraints, and includes every individual in that role's fairness spread.

When a preference term is active, the corresponding \code{P_ta} or \code{P_gr}
element must be present in \code{df_list}.
}
\examples{
inputs <- extract_multirole_info(
  student_df = multirole_students_ex001,
  d_mat = multirole_demand_ex001,
  p_ta_mat = multirole_prefmat_ex001,
  p_gr_mat = multirole_prefmat_ex001,
  e_mode = "rr"
)
model <- prepare_multirole_model(
  inputs,
  alpha_gr = 1,
  beta_gr = 1,
  rho_gr = 10
)

}
